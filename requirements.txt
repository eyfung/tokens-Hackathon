# Core
numpy>=1.24
scipy>=1.11
pydantic>=2.0

# Web
streamlit>=1.28
plotly>=5.17

# Agent SDK (Guild AI — install separately if available)
# guild-ai-agents-sdk>=0.1

# Inference
openai>=1.0  # Pioneer-compatible / DeepMind Gemini

# Vector DB (Actian VectorAI — requires Docker/on-prem)
# actian-vectordb>=0.1  # Install via: docker run -d -p 5432:5432 actian/vectorai

# Communication
band-sdk>=0.1

# QA
replay-browser>=0.1

# Utils
pyyaml>=6.0
python-dotenv>=1.0
rich>=13.0
